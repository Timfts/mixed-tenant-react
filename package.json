{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap --use-workspaces",
    "versionup": "lerna version  --no-git-tag-version --force-publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
